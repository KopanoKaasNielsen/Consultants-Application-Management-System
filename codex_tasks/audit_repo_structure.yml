name: "Audit Repository Hygiene"
auto_execute: true
shell: bash
commands:
  - echo "🔍 Auditing repository structure..."
  - find . -type f -name 'Consultants-Application-Management-System*' -exec echo "⚠️ Duplicate file found:" {} \;
  - find . -type d -empty -exec echo "⚠️ Empty directory:" {} \;
  - find . -type f -name "*.py" -exec grep -Hn "print(" {} \; | grep -v "manage.py" || echo "✅ No stray print statements found."
  - echo "✅ Repository structure audit complete."
